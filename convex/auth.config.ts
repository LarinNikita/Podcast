const authConfig = {
    providers: [
        {
            domain: 'https://equipped-bison-15.clerk.accounts.dev',
            applicationID: 'convex',
        },
    ],
};

export default authConfig;
